<?=$this->htmlA($this->car['url'], $this->car['name'])?>
<?php if (count($this->car['childs'])) { ?>
    <ul>
        <?php foreach ($this->car['childs'] as $car) { ?>
            <?php
                $isTuning = $car['type'] == Car_Parent::TYPE_TUNING;
                $isSport  = $car['type'] == Car_Parent::TYPE_SPORT;
            ?>
            <li class="<?=$isTuning ? 'tuning' : ''?><?=$isSport ? 'sport' : ''?>">
                <?php if ($isTuning) { ?>Тюнинг: <?php } ?>
                <?php if ($isSport) { ?>Спорт: <?php } ?>
                <?=$this->partial('cars/partial/car-tree.phtml', array(
                    'car' => $car
                ))?>
            </li>
        <?php } ?>
    </ul>
<?php } ?>