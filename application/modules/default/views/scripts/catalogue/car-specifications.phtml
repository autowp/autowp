<?php
    $this->layout()->assign(array(
        'blankPage' => false,
        'needLeft'  => true,
        'needRight' => false
    ));
    $title = $this->page->name . ' ' . $this->carList->current()->getFullName();
    $this->headTitle($title, 'SET');
    $this->pageTitle($title);
?>

<?=$this->specifications($this->carList) ?>