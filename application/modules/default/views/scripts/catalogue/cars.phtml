<?php
    $this->layout()->assign(array(
        'blankPage' => false,
        'needLeft'  => true,
        'needRight' => false
    ));
    $title = $this->brandName . ' ' . $this->page->name;
    $this->pageTitle($title);
    $this->headTitle($title, 'SET');
    $this->breadcrumbs()->params(array(
        'BRAND_NAME'    => $this->brandName,
        'BRAND_CATNAME' => $this->brand->folder,
    ));
    if ($this->cartype) {
        $this->breadcrumbs($this->url(array(
            'cartype_catname' => $this->cartype->catname
        ), 'catalogue'), $this->cartype->name);
    }
?>

<ul class="nav nav-pills">
    <?php foreach ($this->cartypes as $cartype) { ?>
        <li<?=$this->cartype && $this->cartype->id == $cartype->id ? ' class="active"' : '' ?>>
            <?=$this->htmlA(array(
                'href' => $this->url(array(
                    'cartype_catname' => $cartype->catname,
                    'page'            => 1
                ), 'catalogue')
            ), $this->escape($cartype->name) . ' <small>('.$cartype->cars_count.')</small>', false) ?>
        </li>
    <?php } ?>
</ul>

<?=$this->cars($this->paginator->getCurrentItems()) ?>
<?=$this->paginationControl($this->paginator) ?>