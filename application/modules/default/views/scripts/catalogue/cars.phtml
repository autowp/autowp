<?php
    if ($this->cartype) {
        $this->pageEnv(array(
            'layout' => array(
                'blankPage' => false,
                'needLeft'  => true,
                'needRight' => false
            ),
            'pageId' => 138,
            'args'   => array(
                'BRAND_CATNAME'    => $this->brand->folder,
                'BRAND_NAME'       => $this->brandName,
                'CAR_TYPE_NAME'    => $this->translate($this->cartype->name),
                'CAR_TYPE_CATNAME' => $this->cartype->catname
            )
        ));
    } else {
        $this->pageEnv(array(
            'layout'    => array(
                'blankPage' => false,
                'needLeft'  => true,
                'needRight' => false
            ),
            'pageId'    => 14,
            'args'      => array(
                'BRAND_CATNAME'  => $this->brand->folder,
                'BRAND_NAME'     => $this->brandName,
            )
        ));
    }
?>

<ul class="nav nav-pills">
    <?php foreach ($this->cartypes as $cartype) { ?>
        <li<?=$this->cartype && $this->cartype->id == $cartype['id'] ? ' class="active"' : '' ?>>
            <?=$this->htmlA(
                    $cartype['url'],
                    $this->escape($this->translate($cartype['name'])) . ' <small>('.$cartype['carsCount'].')</small>',
                    false
            ) ?>
        </li>
    <?php } ?>
</ul>

<?=$this->partial('cars.phtml', $this->listData)?>
<?=$this->paginationControl($this->paginator) ?>