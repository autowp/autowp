<?php
    $this->element->setAttrib('class', 'form-horizontal');
?>
<fieldset>
    <?php if ($legend = $this->element->getLegend()) { ?>
        <legend><?=$this->escape($legend)?></legend>
    <?php } ?>
    <?php foreach ($this->element->getElements() as $element) { ?>
        <?php
            $errors = $element->getMessages();
        ?>
        <div class="row<?=$errors ? ' error' : ''?>">
            <?php if ($element instanceof Zend_Form_Element_Checkbox) { ?>
                <div class="col-offset-3 col-lg-9">
                    <label class="checkbox" for="<?=$element->getId()?>">
                        <?=$this->escape($element->getLabel())?>
                        <?=$element?>
                    </label>
                    <?php foreach ($errors as $error) { ?>
                        <span class="help-inline"><?=$this->escape($error)?></span>
                    <?php } ?>
                </div>
            <?php } else { ?>
                <label class="col-lg-3 control-label" for="<?=$element->getId()?>"><?=$this->escape($element->getLabel())?></label>
                <div class="col-lg-9">
                    <?=$element?>
                    <?php foreach ($errors as $error) { ?>
                        <span class="help-inline"><?=$this->escape($error)?></span>
                    <?php } ?>
                </div>
            <?php } ?>
        </div>
    <?php } ?>
    <div class="row">
        <div class="col-lg-9 col-offset-3">
            <?=$this->formSubmit('submit', 'Войти', array(
                'class' => 'btn btn-primary'
            ))?>
        </div>
    </div>
</fieldset>