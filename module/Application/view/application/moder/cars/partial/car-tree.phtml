<?=$this->htmlA($this->car['url'], $this->car['name'])?>
<?php if (count($this->car['childs'])) { ?>
    <ul>
        <?php foreach ($this->car['childs'] as $car) { ?>
            <?php
                $isTuning = $car['type'] == Application\Model\DbTable\Item\ParentTable::TYPE_TUNING;
                $isSport  = $car['type'] == Application\Model\DbTable\Item\ParentTable::TYPE_SPORT;
            ?>
            <li class="<?=$isTuning ? 'tuning' : ''?><?=$isSport ? 'sport' : ''?>">
                <?php if ($isTuning) { ?><?=$this->escapeHtml($this->translate('catalogue/related'))?>: <?php } ?>
                <?php if ($isSport) { ?><?=$this->escapeHtml($this->translate('catalogue/sport'))?>: <?php } ?>
                <?=$this->partial('application/moder/cars/partial/car-tree', [
                    'car' => $car
                ])?>
            </li>
        <?php } ?>
    </ul>
<?php } ?>