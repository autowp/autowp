<?php foreach ($this->roles as $role) { ?>
    <li>
        <?=sprintf('%02d', $role['id']) ?>. <?=$this->escapeHtml($role['name']) ?>
        <?php if (count($role['childs'])) { ?>
            <ul>
                <?=$this->partial('application/moder/rights/tree', [
                    'roles' => $role['childs']
                ])?>
            </ul>
        <?php } ?>
    </li>
<?php } ?>