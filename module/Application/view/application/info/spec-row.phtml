<?php foreach ($this->items as $item) { ?>
    <tr>
        <td><?=str_repeat('&nbsp;', $this->deep * 4)?><?=$this->escapeHtml($item['short_name'])?></td>
        <td><?=$this->escapeHtml($item['name'])?></td>
    </tr>
    <?=$this->partial('application/info/spec-row', [
        'items' => $item['childs'],
        'deep'  => $this->deep + 1
    ])?>
<?php } ?>