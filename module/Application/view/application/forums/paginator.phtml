<?php if ($this->pageCount > 1) { ?>
    <br />
    <?php foreach ($this->pagesInRange as $page) { ?>
        <?=$this->htmlA($this->url('forums/topic', [
            'topic_id'   => $this->topic_id,
            'page'       => $page
        ]), $page > 1 && $page == $this->firstPageInRange ? '...' : '['. $page . ']') ?>
    <?php } ?>
<?php } ?>